name: Say Hello and Time

on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "🚀 Great job! You're learning fast!"

      - name: Print Current Date & Time
        run: date
